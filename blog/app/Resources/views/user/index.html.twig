{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="content">
      <h1>User list</h1>

      <table>
          <thead>
              <tr>
                  <th>Id</th>
                  <th>username</th>
                  <th>role</th>
                  <th>Actions</th>
              </tr>
          </thead>
          <tbody>
          {% for user in users %}
              <tr>
                  <td><a href="{{ path('article_show', { 'id': user.id }) }}">{{ user.id }}</a></td>
                  <td>{{ user.username }}</td>
                  <td>{{ user.roles[0] }}</td>
                      <ul>
                          <li>
                              <a href="{{ path('article_show', { 'id': user.id }) }}">show</a>
                          </li>
                          <li>
                              <a href="{{ path('article_edit', { 'id': user.id }) }}">edit</a>
                          </li>
                      </ul>
                  </td>
              </tr>
          {% endfor %}
          </tbody>
      </table>

      <ul>
          <li>
              <a href="{{ path('article_new') }}">Create a new entry</a>
          </li>
      </ul>
    </div>
{% endblock %}
