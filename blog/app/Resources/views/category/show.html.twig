{% extends 'base.html.twig' %}

{% block body %}

    <h1>Les articles de la catégorie {{ category.nom}}</h1>
    {% for article in articles %}
            <h2><a href="{{ path('article_show', { 'slug': article.slug }) }}">{{ article.titre }}</a></h2>
            <p>{{ article.resume }}</p>
            <p>{{ article.date|date('Y-m-d') }}</p>
    {% endfor %}


    <a href="{{ path('category_index') }}">Retour a la liste des categories</a>


{% endblock %}
